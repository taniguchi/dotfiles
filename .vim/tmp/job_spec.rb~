require 'spec_helper'

describe Scan::Job do
  pending "add some examples to (or delete) #{__FILE__}"
  before(:each) do
    @job = Scan::Job.new
  end

  it "should be valid" do
    @job.should_not be_valid
  end
  
end
